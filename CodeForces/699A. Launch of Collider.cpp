#include<bits/stdc++.h>

using namespace std;
typedef long long int ll;
vector<pair<char,int> > vp;
int main()
{
    ll n;
    cin >> n;

    while(n--)
    {
        char ch;
        cin >> ch;
        vp.push_back(first_type(ch));

    }



    return 0;
}
