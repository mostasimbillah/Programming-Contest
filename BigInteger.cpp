#include<bits/stdc++.h>

using namespace std;

int main()
{
    string a,b,c,d;

    cin >> a >> b;

    int carry=0;
    int l1=a.length();
    int l2=b.length();
    reverse(a.begin(),a.end());
    reverse(b.begin(),b.end());
    if(l1>l2)
    {

    }
    cout << a << b;

    return 0;
}
